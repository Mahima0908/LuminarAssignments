var lower=Number(prompt("Enter lower number for search"))
var upper=Number(prompt("Enter upper number for search"))

while(lower<=upper){
    if(lower%2==0){
        console.log(lower)
    }
    lower++
}